import { StyleSheet } from "react-native";

// TODO: Add shared layout styles reused across all ecommerce screens.
export const ecommerceStyles = StyleSheet.create({
  screen: {
    flex: 1,
    backgroundColor: "#0B1020",
    paddingHorizontal: 16,
    paddingTop: 10
  },
  section: {
    marginTop: 12,
    gap: 10
  }
});
